micronaut:
  application:
    name: estt-calc
datasources:
  gzzn:
    # TODO use env variables
    url: 'jdbc:oracle:thin:@bj023.int.it2000.com.cn:1521:orcl'
    driverClassName: oracle.jdbc.OracleDriver
    username: ${ORACLE_USER:cdjc}
    password: ${ORACLE_PASS:cdjc}
    dialect: Oracle

default:
  maxHistoryDelay: ${MAX_DELAY:120}
  minHistoryFlight: ${MIN_HISTORY:20}
  dateFormat: 'yyMMdd'
